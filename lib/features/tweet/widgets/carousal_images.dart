import '../../../utils/common/exports.dart';

class CarousalImages extends StatelessWidget {
  const CarousalImages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
